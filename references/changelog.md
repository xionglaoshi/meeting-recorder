# 变更历史

> 各版本改了什么、为什么改。追历史决策时读。

## 历史

- v1.0.14（2026-09-20）：**迁为公用技能（三家共用一份）**。用户口径「Codex/Hermes/dsh 都装了 meeting-recorder，跟 office-preview 一样移到 `~/.agents/` 共用」。①正本从 `~/.codex/skills/meeting-recorder/`（原）迁至 **`~/.agents/skills/meeting-recorder/`**，Hermes/dsh 两份副本移入 `~/.Trash/meeting-recorder-{hermes,dsh}-20260920/`；②**去平台化**：`runtime.py` 解释器候选加 `~/.hermes/venv`、`~/.dsh/venv`（回退链 Codex→Hermes→DSH，本机三者依赖都齐全），`settings.py` 凭证合并链改为「三家 `.env`（codex→hermes→dsh）→ 公用兜底 `~/.agents/.env`」；③`SKILL.md` 新增「归属与位置」节（正本路径、`meeting-up`/`meeting-down` 按需口径、`records/` 与 `vocab.json` 归口、勿在各家重建副本），全部路径引用改指公用区，`.env` 与前端未启动提示（`static/index.html`）同步改文案；④`references/` 三篇活文档（架构 / ASR / 前端）内 22 处路径改指公用区（本 changelog 的历史条目按惯例保留原样，记录当时事实）。迁移**未动任何业务逻辑**：ASR 仍只用阿里百炼 `qwen-audio-3.0-asr-flash-streaming`、纪要仍 `deepseek-flash`。
- v1.0.13（2026-09-13）：**SKILL.md 瘦身 + 中断录音也不留语音**。①**入口瘦身**（用户要求"按规范改好"）：`SKILL.md` 58.5KB → 24KB，把"按需才读"的大段细节拆进 `references/`——`architecture-and-pipeline.md`（架构+代码位置）、`asr-and-model-notes.md`（ASR 引擎/热词/纠错/模型策略/踩坑）、`frontend-notes.md`（前端说明与改造记录）、`changelog.md`（本文件）；`SKILL.md` 只留定位、双模型铁律、生命周期、腾讯会议集成、五步收口、核心 API、已知缺陷/异步契约、运维要点，并加"参考文档"指针表。顺手修正两处标题层级：`已知缺陷`/`异步收尾契约` 原先错嵌在 `## 前端改造需求 v2` 之下，已提升为 `##`。②**中断录音的 pcm 自动删**（用户定稿）：新增 `engine.discard_audio_files()`，录音被中途打断（sox 异常退出）时就地删除该任务的 pcm/wav/mp3（此前会保留，作为 `/api/continue` 续录依据）；`server._startup_maintenance()` 启动时再扫一遍 `records/`，清掉上次崩溃遗留的全部 `会议录音.*` 并打印释放空间。代价：未收口任务不能再续录旧音频（流水保留），这是明确取舍。
- v1.0.12（2026-09-13）：**收口不再留任何语音文件 + stop/regen 异步化 + 真实时长**。①**录音一律不留**（用户定稿，比 v1.0.9 更彻底）：取消"纪要失败/跳过时生成 mp3 兜底"，收口时**无条件**删除 pcm/wav/mp3（含任务目录与 tmp 两处扫描）；纪要失败改走流水 + `/api/regen_minutes` 重生成；仅"既无纪要又无流水"的极端情况打显著警告。②**`/api/stop`、`/api/regen_minutes` 默认异步**：新增 `engine.start_job()`/`mark_processing()` 与 `GET /api/state` 的 `job` 字段（kind/running/ok/error/时间戳），提交即返回、后台线程跑完整链路，`?wait=1` 保留同步语义；`start.sh`/`stop.sh` 改为"POST 后轮询等 state 回 idle"再杀进程（否则会丢纪要）；source 模式自动收尾也走 `start_job`，期间重复 stop 返回 409（原先会跑两遍、白烧 LLM）。③**真实音频时长**：新增 `engine.audio_seconds()`，由已捕获 PCM 字节数推算（16kHz 单声道 int16 → 每秒 32000 字节）并**边采边存快照**（PCM 删掉后不再回落到墙上时钟），替换原先的墙上跨度。④**修 5 个真 BUG**：`proofread` 重复替换（采集+L2 各跑一次 → "平台平台平台""项目砂石开采项目"）、`_chat` 空 content 判断顺序错（推理模型预算被 reasoning 吃光时被误报"模型无输出"）、`_make_minutes` 死代码兜底（`"minutes" in dir(self)` 恒假 → 结构自检两次不过即整条失败）、`extract_title` 只认旧模板（导出文件名"会议纪要纪要"）、导出三连（`Response` 未导入恒 500 + `Content-Disposition` 中文文件名 latin-1 报错 + pandoc `-o -` 拿 HTML 冒充 PDF）。⑤`mark_processing()` 只接受 recording/paused，防重复触发。
- v1.0.11（2026-09-13）：**资产自立化 + 彻底改为按需启动**。①**开机自启全部清除**：`com.xiong.meeting-server` / `com.xiong.office-preview` 两个 LaunchAgent 确认已无残留（plist、launchd disabled 记录、后台项 BTM、登录项、cron 全部干净），本技能原先"服务由 launchd 自启常驻"的说明一并改为"按需启动"；②**去平台依赖**：新增 `runtime.py`（解释器探测：项目 `.venv` → `$AI_AGENT_PYTHON` → `~/.codex/venv`/`~/.codex/venvs/*` → TRAE/Hermes 遗留 → 系统 python3，判据是"依赖能否 import"）与 `settings.py`（路径/密钥/可选数据源解析），全项目不再出现 `~/.hermes` 硬编码；③**依赖合并**：两个项目各自 `.venv`（共 390M）废弃，依赖并入 `~/.codex/venv`（新增 44 包，已有包零版本变动）；④**脚本迁入项目**：`tencent_meeting.py` 等 5 个文件 → `tools/tencent_meeting/`，`hr_etl.py` → 服务根目录（`~/.codex/scripts/hr_etl.py` 改指针存根）；⑤**修 weasyprint**：macOS dyld 找不到 Homebrew GLib 导致 PDF 导出一直失败，现由 `runtime.ensure_native_libs()` 自动补 `DYLD_FALLBACK_LIBRARY_PATH`；⑥**数据脱敏**：`vocab.json`（406 真实人名）/ `hr_etl.py` / `references/*.local.md` 不入库，词表种子抽成 `vocab_seed.py`（不入库）+ `.example` 模板。
- v1.0.9（2026-09-09）：**录音不保留策略（用户定稿）**。①engine.stop 步骤4 后：纪要成功（会议纪要.md 存在）→ 删除 pcm/wav/mp3 并清 files 字典；mp3 生成仅限纪要失败/跳过兜底（不白转）；②server.recover_task 同步删音频；③前端删除全部听录音功能（🎧按钮/audio-bar/播放器 JS/CSS/seek 点击/checkAudio），成功任务只剩流水/纪要按钮；④历史已收口任务音频全部清理（约 1.27GB）；⑤已知缺陷补录：stop 同步阻塞（MapReduce 7+ 分钟前端假死）、export 文件名 topic 提取失败 bug（'会议纪要纪要' + 日期误用收口日，手动修正法见已知缺陷 2）
- v1.0.10（2026-09-09）：**服务外置共享**。`~/.codex/meeting-server/` → `~/.codex/skills/meeting-recorder/`（用户级共享）；产物根 `~/.codex/workspace/meeting/` → `~/.codex/skills/meeting-recorder/records/`（历史 3 条已迁入）；模板/人名职责索引正主 → `~/.codex/skills/meeting-recorder/references/`（技能内留指针）；plist 日志 → `~/.codex/skills/meeting-recorder/logs/`。机制见 `~/.codex/skills/meeting-recorder/README.md` 与 LLM-WIKI [[memories/tools/meeting-server]]。
- v1.0.8（2026-09-02）：**开机自启改造**（⚠️ **已于 2026-09-11 全部废弃，见 v1.0.11**）。①新建 LaunchAgent `~/Library/LaunchAgents/com.xiong.meeting-server.plist`（venv python + WorkingDirectory + PATH 含 /opt/homebrew/bin + RunAtLoad + KeepAlive，日志 `~/.codex/logs/meeting-server.{log,err.log}`），替代原"手动 start.sh、不用常驻"约定；②iCloud 同步 TCC 已实测（launchd 域 venv python 写 LLM-WIKI/reports 成功）；③start.sh 适配托管：优雅停任务→杀进程→等 launchd 拉起（20s 超时），不再自行 exec（避免与 launchd 实例抢 8789 端口）；④office-preview(8765) 对照确认早已自启（com.xiong.office-preview，开机 98s 后运行），无需处理。
- v1.0.7（2026-08-29）：**收尾对齐**。①frontmatter version 从 1.0.0 对齐到实际代码（v1.0.6 后）；②确认并跑通 smoke_test.py 全量自检（环境/模块/链路三层 12 项全过：qwen-audio 引擎就绪、L1 热词已同步、L2 校对、实体核验、MapReduce/模板/说话人/材料端点）；③确认 Speech 兜底前端提示已实现（`asr_error` → "⚠ 转写暂不可用"、`speech_fallback_used` → "本地兜底转写"提示，static/index.html 419-434 行）。待办关账：单测/SKILL 版本/Speech 前端三项全部闭环。
- v1.0.6（2026-08-27）：**目录机制改版：tmp→归档两段式 → 单目录（用户定稿，对齐 dsh mkSessionDir）**。①开始会议即建 `meeting/YYYYMMDDNNN/`（当天序号 001 起），所有过程文件与最终产物全程同目录，结束**不再搬家**；`finalize_archive` 从"LLM 提主题+rename 归档"改为"仅提主题写 metadata.json `topic` 字段"（历史列表显示）；②`_list_tasks`/`broken_tasks`/`recover`/`continue` 全部改为单目录语义（成功=有 metadata.json；失败=有流水/录音但无 metadata），兼容旧 tmp 路径与旧"日期+主题"目录；③启动维护：清理旧 tmp 残留（历史兼容）+ 中断检测改 MEETING_ROOT；④前端历史列表：显示主题+目录号（`链云研发会议 (20260827001)`），失败标签改"未收口"。
- v1.0.5（2026-08-27）：**吸收 dsh meeting-recorder v1.2.0 四个改进**。①**macOS Speech 文件兜底转写**（`asr/asr_macos.swift`+`Info.plist`+`macos_speech.py`+`asr_macos_bin`，SFSpeechURLRecognitionRequest 文件级整段识别）：source 模式 qwen 欠费/断网/结果为空 → 自动调 `_speech_file_fallback()` 本地兜底转写，不再留空流水；`_speech_fallback_used` 标记本次任务是否用过兜底。②**实时专名纠错 rt_correct**（`_append_sentence` 落盘前复用 proofread.load_dict/proofread_text，流水/前端展示即已纠错）。③**2-gram 重叠>0.6 完整句去重**（`deque(maxlen=5)` 窗口，防 qwen 长段重复输出）。④**降级链显式化**：qwen_asr send_audio 成功清 `_error`；pump 循环连续 3 次发送失败 → 显式中断云端+置 `_asr_error`（录音仍保存）。⑤**设备恢复**：start 记录录音前系统默认输入设备，stop 恢复（避免切 iPhone 后留坑）。⚠️ **TCC 授权大坑**：`SFSpeechRecognizer.requestAuthorization` 显式授权触发 TCC「语音识别」usage-description 检查，responsible process（Hermes.app）Info.plist 无 `NSSpeechRecognitionUsageDescription` → 直接 abort（"without a usage description"）。修复：asr_macos 不嵌 plist、不调 requestAuthorization，对齐 swift_asr 隐式授权（继承 responsible process 授权）。dsh 侧无此问题（其环境有独立授权）。proofread.py 后缀吞噬增强：变体展开为 term 后残留组织词（集团/公司/贸易/科技/码头等）自动吞掉（"智豪集团"→"…集团有限公司的"，不再"…集团有限公司集团的"）。
- v1.0.4（2026-08-26）：**录音采集器从 ffmpeg avfoundation 迁移到 sox**（dsh 实测 avfoundation 在 macOS 27 采样掉帧，sox 走 CoreAudio 原生 API 音质更清晰）。①采集命令 `sox -q -d -r 16000 -c 1 -e signed-integer -b 16 -t raw - gain -l 6`（gain -l 6 = +6dB + simple limiter；**sox 无 limiter 滤镜，那是 ffmpeg alimiter 概念**）②设备从 avfoundation 索引 `:0`/`:1` 改为 CoreAudio 设备名，靠 SwitchAudioSource 切换系统默认输入（sox -d 只录默认输入）③变量改名 `_ffmpeg`→`_capture`、`_spawn_ffmpeg`→`_spawn_capture`④`/api/devices` 改 SwitchAudioSource 列设备⑤启动清理/atexit 改杀 sox 进程⑥source 文件模式仍用 ffmpeg 解码（解码不涉及 avfoundation 采集，无掉帧问题）
- v1.0.3（2026-08-20）：**主引擎迁移 qwen-audio-3.0-asr-flash-streaming**（替代 paraformer，官方建议；模型名可在 `settings.py` 改）。①qwen_asr.py 原生 WebSocket run-task 客户端（热词对象格式 `vocabulary:{"词":权重}` + 上下文增强 `input.context` + 增量流 result-generated）②实时流式输出 partial（逐字"正在说"行+闪烁光标）③上下文 person 优先（人名纠错核心）④人名同音变体进字典 L2 校对（把常见同音变体成对写进字典：错字 → 正确写法）⑤补录外部联系人别名⑥假死修复（ffmpeg 启动存活检查 + pump 意外退出置错误）⑦设备动态默认（MacBook 索引动态识别）⑧前端：流式标题「会议流水式记录」+ 纯蓝底矢量话筒 logo + iPhone 提示改短
- v1.0.2（2026-08-19）：前端交互定型。①三按钮+三行基本信息输入（主题/参会/地点）②一键启动引导（探测+提示）③**实时总结彻底废弃**（会议中只流式展示流水）④暂停/恢复在流水插 `⏸/▶` 标记 ⑤纪要模板"一、会议基本信息"读三要素
- v1.0.1（2026-08-19）：五步收口流程 + paraformer 主引擎 + 目录规范（tmp→日期+主题归档）
- v1.0.0（2026-08-19）：浏览器版重构。备份旧插件（pane 形式）在 `~/.codex/workspace/backup/`：`desktop-plugins/meeting-recorder/`（前端）+ `plugins/meeting-recorder/dashboard/`（后端）。弃用原因：①pane 打开方式不友好 ②录音声音小（根因：百炼欠费 + 设备选择 + 无增益）
